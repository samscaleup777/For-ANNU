{
  "name": "annika",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node -e \"console.log('Annïka static site: no build step required')\""
    {
  "scripts": {
    "build": "[my-framework] build --output public"
  }
}
  }
}
